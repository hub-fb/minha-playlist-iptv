name: Atualizar lista de reprodução IPTV

on:
  workflow_dispatch:

jobs:
  teste:
    runs-on: ubuntu-latest

    steps:
      - name: Confira
        uses: actions/checkout@v4

      - name: Teste
        run: echo "Ações do GitHub em execução!"
